# Session Log — 2026-04-01

## What Was Accomplished

- **Sprint 2 (Workspace Hardening)** — Completed and merged to master:
  - `useDesignStatePersistence` hook with 800ms debounce, optimistic concurrency, 409 recovery
  - Save indicator (saving/saved/failed) in workspace Col 1
  - Active project chip with clear button
  - `LoadIntoWorkspaceButton` — client component on project detail page
  - `.vxd` driver import — extracts T/S params, upserts into `driver_database`
  - `inferDriverType` heuristic mapper
  - `onConflictDoUpdate` with `sql\`excluded.*\`` (correct upsert semantics)
  - Workspace `useSearchParams` reads `?activeProject=` param (inside Suspense)

- **Phase 5 — Production Deployment** — LIVE:
  - GitHub repo created: orishavitas/apex-speaker
  - Vercel project linked (project ID: prj_Sb2U1aJF2URxL1dAFw2fm1rc4Pxs)
  - Neon DB connected via Vercel integration
  - Schema pushed (`npx drizzle-kit push`)
  - Production URL: **https://web-blue-theta-12.vercel.app**

- **Knowledge Ingest (23/23)** — All files embedded:
  - Fixed embedder to bypass AI Gateway (use `@ai-sdk/openai` provider directly)
  - Fixed Drizzle vector serialization bug (raw neon client + `::vector(1536)` cast)
  - Fixed chunker 8192-token limit (HARD_CAP_CHARS = 28000)
  - Fixed enum name: `knowledge_status` not `chunk_status`
  - 78 chunks in Neon, HNSW cosine index created

## Decisions Made

- **Embedder uses OPENAI_API_KEY directly, not AI Gateway** — AI Gateway requires per-project billing activation for embeddings. Direct OpenAI provider is simpler and just works. OPENAI_API_KEY lives in `.env.local` (not committed).
- **Raw neon client for embedding inserts** — Drizzle's vector column type is broken for inserts (JSON brace notation). Exported `getNeon()` from `lib/db/index.ts` as escape hatch. This is the canonical fix for any future vector inserts.

## State at Handoff

- Phase: All phases + Sprints 1–2 complete. Production live.
- Blockers: none
- Next action: Start Sprint 3 — implement `calcSealedBox` in `web/lib/types/speaker-math.ts` (the function stub is there, just needs the Butterworth/Chebyshev/Qtc alignment math)

## Files Modified This Session

- `web/lib/knowledge/embedder.ts` — bypass AI Gateway, use `@ai-sdk/openai` directly
- `web/lib/knowledge/upsert.ts` — raw neon SQL for vector insert (Drizzle bug workaround)
- `web/lib/knowledge/chunker.ts` — add HARD_CAP_CHARS=28000 to prevent >8192 token chunks
- `web/lib/db/index.ts` — export `getNeon()` raw client
- `web/lib/constants/workspace.ts` — WORKSPACE_PROJECT_ID singleton
- `web/app/api/design-state/route.ts` — GET auto-upserts projects row (FK constraint fix)
- `web/lib/mappers/infer-driver-type.ts` — new mapper
- `web/lib/mappers/vxd-to-driver-insert.ts` — new mapper
- `web/lib/parser/ts-param-mapper.ts` — NaN guard
- `web/lib/parser/vituixcad-parser.ts` — PARAM array guard
- `web/app/api/upload/route.ts` — .vxd driver import with correct upsert
- `web/components/apex/load-into-workspace-button.tsx` — new client component
- `web/app/dashboard/projects/[id]/page.tsx` — uses LoadIntoWorkspaceButton
- `web/lib/hooks/use-design-state-persistence.ts` — new hook
- `web/app/dashboard/workspace/page.tsx` — wired persistence, save indicator, Suspense

## Context Notes

- The Vercel project is named "web" (not "apex-speaker") — cosmetic issue, low priority
- `OPENAI_API_KEY` is in `.env.local` (gitignored) — needed for re-running `npm run ingest`
- HNSW index already exists — no need to recreate unless schema is wiped
- Sprint 2 recovery: T05 subagent hit usage limit mid-execution; files were unstaged. The `onConflictDoUpdate` bug (self-reference instead of `sql\`excluded.*\``) was found and fixed manually after reviewing `git diff`.
- The RAG system is now fully operational — queries will get relevant context from the 78 ingested chunks
